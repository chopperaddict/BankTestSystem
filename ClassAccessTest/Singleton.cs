namespace ClassAccessTest
{
	/*public class MySingleton
	{

		private static readonly MySingleton instance = new MySingleton ( );
		//private static readonly BankAccount MyBank = new BankAccount ( );

		public delegate void MentionDiscount(decimal subTotal);

		// dummy Constructor so instances cannot be requested
		private MySingleton ( ) { }

		public static BankAccount GetInstance ( )
		{
			//return instance;
			return MyBank;
		}
		public decimal dosomething ( )
		{
			Bank. form1 . ShowText ( "Singleton working ...", null, -1 );
		}
	}
*/
}
